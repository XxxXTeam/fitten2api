[project]
name = "fitten2api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cryptography>=42",
]

[tool.unittest]
start-dir = "tests"
